class Crud {
  constructor(items, rootNode){
    this.items = items;
    this.rootNode = rootNode;
  }
  this.state.items() {
    items;
  };
  this.state.sort () {
    field: 'name',
    direction: 'asc',
  };
  this.state.editingId() {
    return 0;
  };
}
